# Phase A.6 — Controlled Expense UI

- Expense and recurring-expense forms use the shared Moneymatters UI primitives.
- Existing business-scoped tables and RPCs remain the accounting source of truth.
- GST visibility remains driven by the active business tax settings.
- Vendor-required expense heads remain enforced.
- `post_expense()` and `generate_due_recurring_expense()` remain unchanged.
- No schema changes or demo data are introduced by this UI pass.
