# Document settings / bank details

Document preferences support an explicit `show_bank_details` flag and business-scoped document bank account selections. A business may have multiple active bank accounts; document settings must never assume a single account exists.
