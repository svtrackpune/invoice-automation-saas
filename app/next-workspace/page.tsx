'use client';

import { useMemo, useState } from 'react';
import { demoCustomers, demoItems, getDemoInvoices, money, getDemoCustomer, type DemoInvoice } from './test-data';

const go = (href: string) => { window.location.href = href; };
function Card({ children, className = '' }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-3xl border border-slate-200 bg-white shadow-[0_12px_40px_rgba(15,23,42,.045)] ${className}`}>{children}</section>; }
function Metric({ label, value, explanation, trend, tone = 'violet' }: { label: string; value: string; explanation: string; trend?: string; tone?: 'violet'|'emerald'|'amber'|'rose' }) { const tones = { violet: 'bg-violet-50 text-violet-700', emerald: 'bg-emerald-50 text-emerald-700', amber: 'bg-amber-50 text-amber-700', rose: 'bg-rose-50 text-rose-700' }; return <Card className="p-5"><div className="flex items-start justify-between gap-3"><span className="text-xs font-semibold text-slate-500">{label}</span><span className={`rounded-lg px-2 py-1 text-[10px] font-bold ${tones[tone]}`}>{trend || 'Today'}</span></div><div className="mt-3 text-2xl font-bold tracking-tight text-slate-950">{value}</div><p className="mt-1 text-xs leading-5 text-slate-400">{explanation}</p></Card>; }
function Action({ title, description, icon, href }: { title: string; description: string; icon: string; href: string }) { return <button onClick={() => go(href)} className="group rounded-2xl border border-slate-200 p-4 text-left transition hover:-translate-y-0.5 hover:border-violet-200 hover:bg-violet-50/50 hover:shadow-lg"><span className="grid h-11 w-11 place-items-center rounded-xl bg-violet-50 text-lg text-violet-700 transition group-hover:bg-violet-600 group-hover:text-white">{icon}</span><b className="mt-3 block text-sm">{title}</b><span className="mt-1 block text-xs leading-5 text-slate-500">{description}</span></button>; }
function Status({ value }: { value: DemoInvoice['status'] }) { const map: Record<DemoInvoice['status'], string> = { draft: 'bg-slate-100 text-slate-600', sent: 'bg-blue-50 text-blue-700', partially_paid: 'bg-amber-50 text-amber-700', paid: 'bg-emerald-50 text-emerald-700', overdue: 'bg-rose-50 text-rose-700' }; return <span className={`rounded-full px-2.5 py-1 text-[10px] font-bold capitalize ${map[value]}`}>{value.replace('_', ' ')}</span>; }

export default function Dashboard() {
  const [invoices] = useState(getDemoInvoices());
  const [showAll, setShowAll] = useState(false);
  const totals = useMemo(() => { const sales = invoices.reduce((n, i) => n + i.total, 0); const outstanding = invoices.reduce((n, i) => n + Math.max(0, i.total - i.paid), 0); const overdue = invoices.filter(i => i.status === 'overdue'); const overdueValue = overdue.reduce((n, i) => n + Math.max(0, i.total - i.paid), 0); const received = invoices.reduce((n, i) => n + i.paid, 0); return { sales, outstanding, overdue, overdueValue, received }; }, [invoices]);
  const recent = showAll ? invoices : invoices.slice(0, 4);
  return <main className="min-w-0 p-4 sm:p-6 lg:p-8"><div className="mx-auto max-w-[1280px]">
    <div className="mb-7 flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.2em] text-violet-600">Business cockpit</p><h1 className="mt-1 text-3xl font-bold tracking-tight sm:text-4xl">Good evening.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Here is what is happening with your money. You do not need to understand accounting to act on it.</p></div><div className="flex gap-2"><button onClick={() => go('/next-workspace/reports')} className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold">View reports</button><button onClick={() => go('/next-workspace/sales')} className="rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm">＋ New invoice</button></div></div>
    <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Money received" value={money(totals.received)} explanation="Payments already collected from customers." trend="This period" tone="emerald"/><Metric label="Customers owe you" value={money(totals.outstanding)} explanation="Open invoice balances still to collect." trend={`${invoices.length} invoices`} tone="violet"/><Metric label="Overdue" value={money(totals.overdueValue)} explanation="Money that is past its due date." trend={`${totals.overdue.length} overdue`} tone="rose"/><Metric label="Sales" value={money(totals.sales)} explanation="Total value of the current invoice set." trend="This period" tone="amber"/></div>

    <Card className="mt-6 overflow-hidden"><div className="border-b border-slate-100 p-5 sm:p-6"><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-lg font-bold">What do you want to do?</h2><p className="mt-1 text-xs text-slate-500">Start with the business task. Moneymatters keeps the accounting entries in the background.</p></div><span className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Quick actions</span></div></div><div className="grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-3"><Action title="Create invoice" description="Bill a customer and track payment." icon="↗" href="/next-workspace/sales"/><Action title="Create estimate" description="Send a quote before asking for payment." icon="▤" href="/next-workspace/quotation"/><Action title="Record payment" description="Tell Moneymatters money has arrived." icon="₹" href="/next-workspace/payments"/><Action title="Add expense" description="Record money spent by the business." icon="−" href="/next-workspace/expenses"/><Action title="Add customer" description="Save a relationship once, reuse it everywhere." icon="◉" href="/next-workspace/customers"/><Action title="Add product / service" description="Build your catalogue and pricing." icon="□" href="/next-workspace/items"/></div></Card>

    <div className="mt-6 grid gap-6 xl:grid-cols-[1.35fr_.65fr]">
      <Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><h2 className="font-bold">Recent sales</h2><p className="mt-1 text-xs text-slate-500">A simple view of what was billed and what remains.</p></div><button onClick={() => setShowAll(v => !v)} className="text-xs font-bold text-violet-700">{showAll ? 'Show less' : 'View all'}</button></div><div className="divide-y divide-slate-100">{recent.map(invoice => { const customer = getDemoCustomer(invoice.customerId); const balance = Math.max(0, invoice.total - invoice.paid); return <button key={invoice.id} onClick={() => go('/next-workspace/sales')} className="flex w-full items-center gap-3 px-5 py-4 text-left hover:bg-violet-50/40"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-violet-50 text-violet-700">₹</span><span className="min-w-0 flex-1"><b className="block truncate text-sm">{invoice.number}</b><span className="text-xs text-slate-500">{customer.name} · {invoice.date}</span></span><span className="hidden text-right sm:block"><b className="block text-sm">{money(invoice.total)}</b><span className="text-[10px] text-slate-400">{balance ? `${money(balance)} remaining` : 'Fully paid'}</span></span><Status value={invoice.status}/><span className="text-slate-300">›</span></button>; })}</div></Card>

      <div className="space-y-6"><Card className="p-5"><h2 className="font-bold">Needs attention</h2><p className="mt-1 text-xs text-slate-500">Only the things worth looking at right now.</p><div className="mt-4 space-y-2">{totals.overdue.length ? <button onClick={() => go('/next-workspace/sales')} className="flex w-full items-center justify-between rounded-2xl bg-rose-50 p-4 text-left"><span><b className="block text-sm text-rose-900">Collect {money(totals.overdueValue)}</b><span className="mt-1 block text-xs text-rose-700">{totals.overdue.length} invoice{totals.overdue.length > 1 ? 's are' : ' is'} overdue.</span></span><span className="font-bold text-rose-700">→</span></button> : <div className="rounded-2xl bg-emerald-50 p-4"><b className="block text-sm text-emerald-900">You are all clear</b><span className="mt-1 block text-xs text-emerald-700">No overdue invoices in the demo data.</span></div>}<button onClick={() => go('/next-workspace/banking')} className="flex w-full items-center justify-between rounded-2xl bg-violet-50 p-4 text-left"><span><b className="block text-sm text-violet-900">Review banking</b><span className="mt-1 block text-xs text-violet-700">Match transactions and keep books accurate.</span></span><span className="font-bold text-violet-700">→</span></button><button onClick={() => go('/next-workspace/tax')} className="flex w-full items-center justify-between rounded-2xl bg-amber-50 p-4 text-left"><span><b className="block text-sm text-amber-900">Tax center</b><span className="mt-1 block text-xs text-amber-700">See what your current books say about tax.</span></span><span className="font-bold text-amber-700">→</span></button></div></Card><Card className="p-5"><h2 className="font-bold">Your business, explained</h2><p className="mt-2 text-xs leading-5 text-slate-500">Moneymatters turns accounting terms into decisions. Open a number anywhere to see where it came from and what you can do next.</p><button onClick={() => go('/next-workspace/reports')} className="mt-4 rounded-xl border border-slate-200 px-3 py-2 text-xs font-bold hover:bg-slate-50">Explore insights →</button></Card></div>
    </div>

    <div className="mt-6 rounded-3xl border border-dashed border-violet-200 bg-violet-50/50 p-5 sm:p-6"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-violet-600">Moneymatters principle</p><h2 className="mt-1 text-lg font-bold">Explain → Suggest → Act</h2><p className="mt-1 max-w-2xl text-xs leading-5 text-slate-600">Every important number should tell you what it means, why it changed and what you can do next. Advanced accounting stays available without making everyday work complicated.</p></div><button onClick={() => go('/next-workspace/accounting')} className="shrink-0 rounded-xl bg-white px-4 py-2.5 text-xs font-bold text-violet-700 shadow-sm">Open accounting mode →</button></div></div>
  </div></main>;
}
