'use client';

import { useEffect, useMemo, useState } from 'react';
import { supabase, type BusinessContext } from '@/lib/supabase';

type Customer={id:string;display_name:string;phone:string|null;email:string|null};
type Invoice={id:string;invoice_number:string;invoice_date:string;status:string;total:number;balance_due:number;customer_id:string};
type Product={id:string;name:string;sku:string|null;sales_price:number;inventory_tracked:boolean};
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(Number(n||0));
const date=(v:string)=>new Intl.DateTimeFormat('en-IN',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(v));
const nav=[['Overview','/next-workspace','⌂'],['Sales','/next-workspace/sales','↗'],['Quotations','/next-workspace/quotation','▤'],['Customers','/next-workspace/customers','♙'],['Inventory','/next-workspace/inventory','▦'],['Accounting','/next-workspace/accounting','∑'],['Tax & ITR','/next-workspace/tax','%'],['Reports','/next-workspace/reports','▥'],['Branding','/next-workspace/brand','✦'],['Preferences','/next-workspace/preferences','⚙']];
function Card({children,className='' }:{children:React.ReactNode;className?:string}){return <section className={`rounded-2xl border border-slate-200 bg-white shadow-[0_8px_30px_rgba(70,60,120,.06)] ${className}`}>{children}</section>}
function Status({value}:{value:string}){const v=value.toLowerCase();const cls=v==='paid'?'bg-emerald-50 text-emerald-700':v==='overdue'?'bg-rose-50 text-rose-700':v==='sent'?'bg-violet-50 text-violet-700':'bg-slate-100 text-slate-600';return <span className={`rounded-full px-2.5 py-1 text-[11px] font-semibold ${cls}`}>{value.replaceAll('_',' ')}</span>}

export default function NextWorkspace(){
 const[session,setSession]=useState<any>(null),[contexts,setContexts]=useState<BusinessContext[]>([]),[ctx,setCtx]=useState<BusinessContext|null>(null),[loading,setLoading]=useState(true);
 const loadContexts=async(sessionUser?:any)=>{const r=await supabase.rpc('get_my_business_context');const rows=(r.data||[]) as BusinessContext[];setContexts(rows);if(r.error||!rows.length){setCtx(null);return}const uid=sessionUser?.id||session?.user?.id;const profile=uid?await supabase.from('user_profiles').select('active_business_id').eq('user_id',uid).maybeSingle():{data:null};const saved=profile.data?.active_business_id||(typeof window!=='undefined'?localStorage.getItem('moneymatters.activeBusinessId'):null);const selected=rows.find(x=>x.business_id===saved)||rows[0];setCtx(selected);if(typeof window!=='undefined')localStorage.setItem('moneymatters.activeBusinessId',selected.business_id);if(uid&&selected.business_id!==profile.data?.active_business_id)await supabase.from('user_profiles').update({active_business_id:selected.business_id}).eq('user_id',uid)};
 useEffect(()=>{let mounted=true;supabase.auth.getSession().then(async({data})=>{if(!mounted)return;setSession(data.session);if(data.session)await loadContexts(data.session.user);setLoading(false)});const{data:{subscription}}=supabase.auth.onAuthStateChange(async(_e,s)=>{setSession(s);if(s)await loadContexts(s.user);else{setContexts([]);setCtx(null)}});return()=>{mounted=false;subscription.unsubscribe()}},[]);
 const selectBusiness=async(id:string)=>{const next=contexts.find(x=>x.business_id===id);if(!next)return;setCtx(next);localStorage.setItem('moneymatters.activeBusinessId',id);if(session?.user?.id)await supabase.from('user_profiles').update({active_business_id:id}).eq('user_id',session.user.id)};
 if(loading)return <div className="grid min-h-screen place-items-center bg-[#fbfaff] text-sm text-slate-500">Loading your workspace…</div>;
 if(!session)return <Gate title="Sign in required" text="Sign in to Moneymatters to open your business workspace." action="Open Moneymatters" onClick={()=>location.href='/'}/>;
 if(!ctx)return <Gate title="Finish business setup" text="Your account is signed in, but no business workspace is available yet." action="Open setup" onClick={()=>location.href='/'}/>;
 return <Workspace ctx={ctx} contexts={contexts} onBusinessChange={selectBusiness}/>;
}
function Gate({title,text,action,onClick}:{title:string;text:string;action:string;onClick:()=>void}){return <main className="grid min-h-screen place-items-center bg-[#fbfaff] p-6"><Card className="max-w-lg p-8 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-violet-600 font-black text-white">M</div><h1 className="mt-5 text-2xl font-semibold">{title}</h1><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p><button onClick={onClick} className="mt-6 rounded-xl bg-violet-600 px-5 py-3 text-sm font-semibold text-white shadow-sm">{action}</button></Card></main>}
function Workspace({ctx,contexts,onBusinessChange}:{ctx:BusinessContext;contexts:BusinessContext[];onBusinessChange:(id:string)=>void}){
 const[customers,setCustomers]=useState<Customer[]>([]),[invoices,setInvoices]=useState<Invoice[]>([]),[products,setProducts]=useState<Product[]>([]),[search,setSearch]=useState(''),[showCreate,setShowCreate]=useState(false),[mobile,setMobile]=useState(false),[refresh,setRefresh]=useState(0),[notice,setNotice]=useState('');
 const load=async()=>{const[c,i,p]=await Promise.all([supabase.from('customers').select('id,display_name,phone,email').eq('business_id',ctx.business_id).eq('is_active',true).order('created_at',{ascending:false}).limit(100),supabase.from('invoices').select('id,invoice_number,invoice_date,status,total,balance_due,customer_id').eq('business_id',ctx.business_id).order('created_at',{ascending:false}).limit(100),supabase.from('products_services').select('id,name,sku,sales_price,inventory_tracked').eq('business_id',ctx.business_id).eq('is_active',true).order('created_at',{ascending:false}).limit(100)]);if(c.error||i.error||p.error)setNotice((c.error||i.error||p.error)?.message||'Some dashboard data could not be loaded.');setCustomers(c.data||[]);setInvoices(i.data||[]);setProducts(p.data||[])};
 useEffect(()=>{load()},[refresh,ctx.business_id]);
 const metrics=useMemo(()=>{const sales=invoices.reduce((a,x)=>a+Number(x.total||0),0);const receivables=invoices.reduce((a,x)=>a+Number(x.balance_due||0),0);const overdue=invoices.filter(x=>x.status==='overdue');const paid=invoices.filter(x=>x.status==='paid');return{sales,receivables,overdue,paid,customers:customers.length,products:products.length}},[invoices,customers,products]);
 const customerName=(id:string)=>customers.find(x=>x.id===id)?.display_name||'Customer';
 const filtered=invoices.filter(x=>`${x.invoice_number} ${customerName(x.customer_id)} ${x.status}`.toLowerCase().includes(search.toLowerCase()));
 const go=(target:string)=>location.href=target;
 const actions=[['Invoice','Create and send an invoice','/next-workspace/sales','＋'],['Quotation','Prepare an estimate for approval','/next-workspace/quotation','▤'],['Customer','Add a customer','/next-workspace/customers','♙'],['Product / Service','Add an item or service','/next-workspace/inventory','▦']];
 return <div className="min-h-screen bg-[#fbfaff] text-slate-950">
  <header className="sticky top-0 z-40 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="mx-auto flex h-[68px] max-w-[1500px] items-center gap-3 px-4 lg:px-6"><button onClick={()=>setMobile(!mobile)} className="grid h-10 w-10 place-items-center rounded-xl border border-slate-200 bg-white lg:hidden">☰</button><button onClick={()=>go('/next-workspace')} className="flex items-center gap-3 text-left"><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-600 text-sm font-black text-white">M</span><span className="hidden sm:block"><b className="block text-sm tracking-tight">Moneymatters</b><small className="block max-w-[190px] truncate text-[10px] text-slate-500">{ctx.business_name}</small></span></button>{contexts.length>1&&<select value={ctx.business_id} onChange={e=>onBusinessChange(e.target.value)} aria-label="Business" className="max-w-[190px] rounded-xl border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-700"><option value={ctx.business_id}>{ctx.business_name}</option>{contexts.filter(x=>x.business_id!==ctx.business_id).map(x=><option key={x.business_id} value={x.business_id}>{x.business_name}</option>)}</select>}<div className="mx-auto hidden w-full max-w-xl md:block"><div className="relative"><span className="absolute left-3 top-2.5 text-slate-400">⌕</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search invoices, customers or products" className="w-full rounded-xl border border-slate-200 bg-[#fbfaff] py-2.5 pl-9 pr-4 text-sm outline-none"/></div></div><div className="relative ml-auto"><button onClick={()=>setShowCreate(!showCreate)} className="rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-violet-700">＋ Create</button>{showCreate&&<div className="absolute right-0 mt-2 w-80 rounded-2xl border border-slate-200 bg-white p-2 shadow-xl">{actions.map(([label,desc,target,icon])=><button key={label} onClick={()=>{setShowCreate(false);go(target)}} className="flex w-full items-center gap-3 rounded-xl p-3 text-left hover:bg-violet-50"><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-50 text-violet-700">{icon}</span><span><b className="block text-sm">{label}</b><small className="text-xs text-slate-500">{desc}</small></span></button>)}</div>}</div></div></header>
  <div className="mx-auto grid max-w-[1500px] lg:grid-cols-[230px_1fr]"><aside className={`${mobile?'fixed inset-y-[68px] left-0 z-30 block w-[280px] shadow-xl':'hidden'} border-r border-slate-200 bg-white p-3 lg:static lg:block lg:min-h-[calc(100vh-68px)]`}><p className="px-3 pb-3 pt-2 text-[10px] font-bold uppercase tracking-[.18em] text-slate-400">Workspace</p>{nav.map(([label,href,icon])=><button key={href} onClick={()=>{setMobile(false);go(href)}} className={`mb-1 flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm font-medium ${href==='/next-workspace'?'bg-violet-50 text-violet-800':'text-slate-600 hover:bg-violet-50 hover:text-violet-800'}`}><span className="grid w-5 place-items-center text-sm">{icon}</span>{label}</button>)}<div className="mt-6 rounded-2xl border border-violet-100 bg-violet-50 p-4"><p className="text-xs font-bold text-violet-900">Simple on the surface</p><p className="mt-1 text-xs leading-5 text-violet-700">Moneymatters keeps accounting connected in the background.</p></div></aside>
  <main className="min-w-0 p-4 sm:p-6 lg:p-8"><div className="mb-7 flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-violet-600">Overview</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Good {new Date().getHours()<12?'morning':new Date().getHours()<17?'afternoon':'evening'}, {ctx.business_name}.</h1><p className="mt-2 text-sm text-slate-500">Here is what needs your attention today.</p></div><div className="flex gap-2"><button onClick={()=>setRefresh(x=>x+1)} className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 hover:bg-violet-50">↻ Refresh</button><button onClick={()=>go('/next-workspace/sales')} className="rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-semibold text-white">Create invoice</button></div></div>
   {notice&&<div className="mb-5 rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800">{notice}</div>}
   <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric title="Sales" value={money(metrics.sales)} note="From loaded invoices"/><Metric title="Amount to collect" value={money(metrics.receivables)} note={`${metrics.overdue.length} overdue`} tone="violet"/><Metric title="Customers" value={String(metrics.customers)} note="Active customers"/><Metric title="Products & services" value={String(metrics.products)} note="Active catalogue"/></div>
   <div className="mt-6"><div className="mb-3 flex items-center justify-between"><div><h2 className="text-lg font-semibold">Quick actions</h2><p className="text-xs text-slate-500">The four things you are most likely to do.</p></div></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{actions.map(([label,desc,target,icon])=><button key={label} onClick={()=>go(target)} className="group rounded-2xl border border-slate-200 bg-white p-4 text-left shadow-sm transition hover:-translate-y-0.5 hover:border-violet-200 hover:shadow-md"><span className="grid h-10 w-10 place-items-center rounded-xl bg-violet-50 text-violet-700 group-hover:bg-violet-100">{icon}</span><b className="mt-3 block text-sm">{label}</b><span className="mt-1 block text-xs leading-5 text-slate-500">{desc}</span></button>)}</div></div>
   <div className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_.6fr]"><Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><h2 className="font-semibold">Recent invoices</h2><p className="mt-1 text-xs text-slate-500">Click an invoice to continue the customer flow.</p></div><button onClick={()=>go('/next-workspace/sales')} className="text-xs font-semibold text-violet-700">View all →</button></div>{filtered.length===0?<div className="p-8 text-center text-sm text-slate-500">No invoices yet. Create your first invoice to get started.</div>:<div className="divide-y divide-slate-100">{filtered.slice(0,8).map(i=><button key={i.id} onClick={()=>go('/next-workspace/sales')} className="flex w-full items-center gap-4 px-5 py-4 text-left hover:bg-violet-50/40"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-violet-50 text-violet-700">₹</span><span className="min-w-0 flex-1"><b className="block truncate text-sm">{i.invoice_number}</b><span className="text-xs text-slate-500">{customerName(i.customer_id)} · {date(i.invoice_date)}</span></span><span className="hidden text-right sm:block"><b className="block text-sm">{money(i.total)}</b><span className="text-xs text-slate-400">{money(i.balance_due)} due</span></span><Status value={i.status}/><span className="text-slate-300">›</span></button>)}</div>}</Card>
    <div className="space-y-6"><Card className="p-5"><h2 className="font-semibold">Needs attention</h2><div className="mt-4 space-y-3"><button onClick={()=>go('/next-workspace/sales')} className="flex w-full items-center justify-between rounded-xl bg-rose-50 p-3 text-left"><span><b className="block text-sm text-rose-900">Overdue invoices</b><span className="text-xs text-rose-700">Customers who need a reminder</span></span><b className="text-lg text-rose-700">{metrics.overdue.length}</b></button><button onClick={()=>go('/next-workspace/accounting')} className="flex w-full items-center justify-between rounded-xl bg-violet-50 p-3 text-left"><span><b className="block text-sm text-violet-900">Accounting</b><span className="text-xs text-violet-700">Review books and balances</span></span><span className="text-violet-700">→</span></button></div></Card><Card className="p-5"><h2 className="font-semibold">Your workflow</h2><p className="mt-2 text-xs leading-5 text-slate-500">Customer → Quotation → Invoice → Payment → Receipt</p><button onClick={()=>go('/next-workspace/customers')} className="mt-4 w-full rounded-xl border border-violet-200 bg-violet-50 px-4 py-2.5 text-sm font-semibold text-violet-800">Open customers</button></Card></div></div>
  </main></div></div>;
}
function Metric({title,value,note,tone='normal'}:{title:string;value:string;note:string;tone?:'normal'|'violet'}){return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><span className="text-xs font-medium text-slate-500">{title}</span><span className={`grid h-8 w-8 place-items-center rounded-xl ${tone==='violet'?'bg-violet-50 text-violet-700':'bg-slate-50 text-slate-500'}`}>•</span></div><div className="mt-3 text-2xl font-semibold tracking-tight">{value}</div><p className="mt-1 text-xs text-slate-500">{note}</p></section>}
