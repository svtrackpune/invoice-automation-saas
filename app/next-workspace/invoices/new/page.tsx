'use client';

import { useEffect, useMemo, useState } from 'react';
import { supabase, type BusinessContext } from '@/lib/supabase';
import ItemServiceModal from './ItemServiceModal';
import CustomerCreateModal from '../../customers/CustomerCreateModal';
import type { CreatedCustomer } from '../../customers/CustomerCreateModal';

type Customer = { id:string; display_name:string; legal_name:string|null; email:string|null; phone:string|null; website:string|null; payment_terms_days:number; payment_reminders_enabled:boolean; reminder_days_before_due:number; default_discount_type:string; default_discount_value:number; billing_address:any; shipping_address:any };
type Product = { id:string; name:string; sku:string|null; description:string|null; item_type:string; unit:string|null; sales_price:number; default_tax_rate_id:string|null; discount_enabled:boolean; max_discount_type:string; max_discount_value:number };
type Tax = { id:string; name:string; rate:number };
type Template = { id:string; template_name:string };
type Bank = { id:string; name:string; institution_name:string|null; account_last4:string|null };
type Line = { product_service_id:string; description:string; quantity:number; unit_price:number; discount_type:string; discount_value:number; tax_rate_id:string };
type TaxProfile = { tax_regime:string|null; gst_registration_type:string|null };
type DocumentDefaults = { invoice_due_days:number; invoice_notes:string|null; default_payment_terms:string|null; invoice_template_id:string|null };
type PaymentDisplayMode = 'none'|'bank'|'online';
type NewItemKind = 'product'|'service';

const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:2}).format(Number(n||0));
const today=()=>new Date().toISOString().slice(0,10);
const plusDays=(n:number)=>new Date(Date.now()+Math.max(0,n)*86400000).toISOString().slice(0,10);
const normalizeDiscountType=(v:string|null|undefined)=>v==='percent'?'percentage':v==='amount'?'amount':v==='percentage'?'percentage':'';

function Input(p:React.InputHTMLAttributes<HTMLInputElement>){return <input {...p} className={`w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm outline-none focus:border-violet-400 focus:ring-2 focus:ring-violet-100 ${p.className||''}`}/>}
function Select(p:React.SelectHTMLAttributes<HTMLSelectElement>){return <select {...p} className={`w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm outline-none focus:border-violet-400 focus:ring-2 focus:ring-violet-100 ${p.className||''}`}/>}
function Button({children,onClick,secondary=false,disabled=false,className=''}:{children:React.ReactNode;onClick?:()=>void;secondary?:boolean;disabled?:boolean;className?:string}){return <button type="button" disabled={disabled} onClick={onClick} className={`rounded-xl px-4 py-2.5 text-sm font-semibold disabled:opacity-50 ${secondary?'border border-slate-200 bg-white text-slate-700 hover:bg-slate-50':'bg-violet-600 text-white hover:bg-violet-700'} ${className}`}>{children}</button>}
function Field({label,children,wide=false,required=false,help}:{label:string;children:React.ReactNode;wide?:boolean;required?:boolean;help?:string}){return <label className={`${wide?'sm:col-span-2 ':''}block text-xs font-semibold text-slate-600`}>{label}{required?' *':''}<div className="mt-1">{children}</div>{help&&<p className="mt-1 text-[11px] font-normal text-slate-400">{help}</p>}</label>}
function Card({children,className='' }:{children:React.ReactNode;className?:string}){return <section className={`rounded-3xl border border-slate-200 bg-white shadow-sm ${className}`}>{children}</section>}

export default function NewInvoice(){
 const editId=typeof window!=='undefined'?new URLSearchParams(window.location.search).get('edit'):null;
 const [ctx,setCtx]=useState<BusinessContext|null>(null),[customers,setCustomers]=useState<Customer[]>([]),[products,setProducts]=useState<Product[]>([]),[taxes,setTaxes]=useState<Tax[]>([]),[templates,setTemplates]=useState<Template[]>([]),[banks,setBanks]=useState<Bank[]>([]),[taxProfile,setTaxProfile]=useState<TaxProfile>({tax_regime:'NONE',gst_registration_type:'NONE'}),[defaults,setDefaults]=useState<DocumentDefaults>({invoice_due_days:0,invoice_notes:'',default_payment_terms:'',invoice_template_id:null});
 const [customerId,setCustomerId]=useState(''),[invoiceDate,setInvoiceDate]=useState(today()),[dueDate,setDueDate]=useState(today()),[lines,setLines]=useState<Line[]>([]),[discountEnabled,setDiscountEnabled]=useState(false),[discountType,setDiscountType]=useState(''),[discountValue,setDiscountValue]=useState(0),[gstEnabled,setGstEnabled]=useState(false),[notes,setNotes]=useState(''),[terms,setTerms]=useState(''),[template,setTemplate]=useState(''),[paymentMode,setPaymentMode]=useState<PaymentDisplayMode>('none'),[paymentBankId,setPaymentBankId]=useState('');
 const [busy,setBusy]=useState(false),[error,setError]=useState(''),[customerFormOpen,setCustomerFormOpen]=useState(false),[itemFormOpen,setItemFormOpen]=useState(false),[itemChooserOpen,setItemChooserOpen]=useState(false),[newItemKind,setNewItemKind]=useState<NewItemKind>('product'),[editLoaded,setEditLoaded]=useState(!editId);

 useEffect(()=>{let mounted=true;(async()=>{
  const contextResult=await supabase.rpc('get_my_business_context');const business=contextResult.data?.[0] as BusinessContext|undefined;if(!business){location.href='/';return}if(!mounted)return;setCtx(business);
  const [cs,ps,ts,tp,ds,dp,tm,ba]=await Promise.all([
   supabase.from('customers').select('id,display_name,legal_name,email,phone,website,payment_terms_days,payment_reminders_enabled,reminder_days_before_due,default_discount_type,default_discount_value,billing_address,shipping_address').eq('business_id',business.business_id).eq('is_active',true).order('display_name'),
   supabase.from('products_services').select('id,name,sku,description,item_type,unit,sales_price,default_tax_rate_id,discount_enabled,max_discount_type,max_discount_value').eq('business_id',business.business_id).eq('is_active',true).eq('sell_enabled',true).order('name'),
   supabase.from('tax_rates').select('id,name,rate').eq('business_id',business.business_id).eq('is_active',true).order('rate'),
   supabase.from('business_tax_profiles').select('tax_regime,gst_registration_type').eq('business_id',business.business_id).maybeSingle(),
   supabase.from('business_settings').select('invoice_due_days,invoice_notes,default_payment_terms').eq('business_id',business.business_id).maybeSingle(),
   supabase.from('business_document_preferences').select('template_id').eq('business_id',business.business_id).eq('document_type','invoice').maybeSingle(),
   supabase.from('document_templates').select('id,template_name').eq('document_type','invoice').eq('is_active',true).order('template_name'),
   supabase.from('bank_accounts').select('id,name,institution_name,account_last4').eq('business_id',business.business_id).eq('is_active',true).order('name')
  ]);if(!mounted)return;
  const profile=(tp.data||{tax_regime:'NONE',gst_registration_type:'NONE'}) as TaxProfile;const regime=String(profile.tax_regime||'NONE').toUpperCase();const eligible=regime!=='NONE'&&!(regime==='GST'&&String(profile.gst_registration_type||'NONE').toUpperCase()==='COMPOSITION');
  setCustomers((cs.data||[]) as Customer[]);setProducts((ps.data||[]) as Product[]);setTaxes((ts.data||[]) as Tax[]);setTemplates((tm.data||[]) as Template[]);setBanks((ba.data||[]) as Bank[]);setTaxProfile(profile);setGstEnabled(eligible);
  const d=(ds.data||{}) as any;const documentDefaults:DocumentDefaults={invoice_due_days:Number(d.invoice_due_days||0),invoice_notes:d.invoice_notes||'',default_payment_terms:d.default_payment_terms||'',invoice_template_id:dp.data?.template_id||null};setDefaults(documentDefaults);setDueDate(plusDays(documentDefaults.invoice_due_days));setNotes(documentDefaults.invoice_notes||'');setTerms(documentDefaults.default_payment_terms||'');setTemplate(documentDefaults.invoice_template_id||'');
  if(editId){const [ir,itemsResult]=await Promise.all([supabase.from('invoices').select('id,customer_id,invoice_date,due_date,notes,terms,discount_type,discount_value,template_id,status,payment_display_mode,payment_bank_account_id').eq('id',editId).eq('business_id',business.business_id).maybeSingle(),supabase.from('invoice_items').select('product_service_id,description,quantity,unit_price,discount_type,discount_value,tax_rate_id').eq('invoice_id',editId).order('sort_order')]);if(ir.error||itemsResult.error){setError(ir.error?.message||itemsResult.error?.message||'Unable to load invoice.');setEditLoaded(true);return}if(!ir.data){setError('Invoice not found.');setEditLoaded(true);return}if(ir.data.status!=='draft'){setError('Only draft invoices can be edited. This invoice has already been posted or paid.');setEditLoaded(true);return}setCustomerId(ir.data.customer_id||'');setInvoiceDate(ir.data.invoice_date||today());setDueDate(ir.data.due_date||plusDays(documentDefaults.invoice_due_days));setNotes(ir.data.notes||'');setTerms(ir.data.terms||documentDefaults.default_payment_terms||'');const loadedDiscountType=normalizeDiscountType(ir.data.discount_type);const loadedDiscountValue=Number(ir.data.discount_value||0);setDiscountType(loadedDiscountType);setDiscountValue(loadedDiscountValue);setDiscountEnabled(Boolean(loadedDiscountType));setTemplate(ir.data.template_id||documentDefaults.invoice_template_id||'');setPaymentMode((ir.data.payment_display_mode||'none') as PaymentDisplayMode);setPaymentBankId(ir.data.payment_bank_account_id||'');setLines((itemsResult.data||[]).map((x:any)=>({product_service_id:x.product_service_id||'',description:x.description||'',quantity:Number(x.quantity||1),unit_price:Number(x.unit_price||0),discount_type:normalizeDiscountType(x.discount_type),discount_value:Number(x.discount_value||0),tax_rate_id:x.tax_rate_id||''})));}
  setEditLoaded(true)
 })();return()=>{mounted=false}},[editId]);

 const taxRegime=String(taxProfile.tax_regime||'NONE').toUpperCase(),gstType=String(taxProfile.gst_registration_type||'NONE').toUpperCase(),composition=taxRegime==='GST'&&gstType==='COMPOSITION',gstEligible=taxRegime==='GST'&&!composition,taxEligible=taxRegime!=='NONE'&&!composition,taxEnabled=taxEligible&&gstEnabled,customer=customers.find(x=>x.id===customerId);
 const sortedItems=useMemo(()=>[...products].sort((a,b)=>a.name.localeCompare(b.name,undefined,{sensitivity:'base'})),[products]);
 const addLine=(kind:NewItemKind)=>{const p=sortedItems.find(x=>x.item_type===kind);if(!p){setNewItemKind(kind);setItemFormOpen(true);return}setLines(x=>[...x,{product_service_id:p.id,description:p.description||p.name,quantity:1,unit_price:Number(p.sales_price||0),discount_type:'',discount_value:0,tax_rate_id:taxEnabled?(p.default_tax_rate_id||''):''}])};
 const openNewItem=(kind:NewItemKind)=>{setItemChooserOpen(false);setNewItemKind(kind);setItemFormOpen(true)};
 const changeLine=(i:number,key:keyof Line,value:string|number)=>setLines(x=>x.map((line,j)=>j===i?{...line,[key]:value}:line));
 const lineDiscount=(line:Line,product?:Product)=>{if(!product?.discount_enabled)return 0;const base=Number(line.quantity||0)*Number(line.unit_price||0);let value=line.discount_type==='percentage'?base*Number(line.discount_value||0)/100:Number(line.discount_value||0);const maxType=normalizeDiscountType(product.max_discount_type);if(Number(product.max_discount_value||0)>0){const max=maxType==='percentage'?base*Number(product.max_discount_value)/100:Number(product.max_discount_value);value=Math.min(value,max)}return Math.max(0,Math.min(base,value))};
 const totals=useMemo(()=>{let subtotal=0,discount=0,tax=0;for(const line of lines){const product=products.find(x=>x.id===line.product_service_id);const base=Number(line.quantity||0)*Number(line.unit_price||0);const d=lineDiscount(line,product);const rate=taxEnabled?(taxes.find(t=>t.id===line.tax_rate_id)?.rate||0):0;subtotal+=base;discount+=d;tax+=(base-d)*rate/100}const base=Math.max(0,subtotal-discount);const invoiceDiscount=discountEnabled?(discountType==='percentage'?base*Number(discountValue||0)/100:Number(discountValue||0)):0;const applied=Math.min(base,Math.max(0,invoiceDiscount));return{subtotal,discount:discount+applied,tax,total:Math.max(0,subtotal-discount-applied+tax)}},[lines,products,taxes,discountEnabled,discountType,discountValue,taxEnabled]);
 const selectCustomer=(id:string)=>{setCustomerId(id);const c=customers.find(x=>x.id===id);if(c){setDueDate(plusDays(Number(c.payment_terms_days||defaults.invoice_due_days||0)));const dt=normalizeDiscountType(c.default_discount_type);const dv=Number(c.default_discount_value||0);setDiscountType(dt);setDiscountValue(dv);setDiscountEnabled(Boolean(dt));}};
 const toggleDiscount=(enabled:boolean)=>{setDiscountEnabled(enabled);if(!enabled){setDiscountType('');setDiscountValue(0)}else if(!discountType)setDiscountType('percentage')};
 const onCustomerCreated=(c:CreatedCustomer)=>{setCustomers(x=>[...x,c as Customer].sort((a,b)=>a.display_name.localeCompare(b.display_name)));setCustomerId(c.id);setCustomerFormOpen(false)};
 const onItemCreated=(item:Product)=>{setProducts(x=>[...x,item].sort((a,b)=>a.name.localeCompare(b.name)));setLines(x=>[...x,{product_service_id:item.id,description:item.description||item.name,quantity:1,unit_price:Number(item.sales_price||0),discount_type:'',discount_value:0,tax_rate_id:taxEnabled?(item.default_tax_rate_id||''):''}]);setItemFormOpen(false)};
 const saveInvoice=async()=>{if(!ctx||!customerId||!lines.length||totals.total<=0)return;if(paymentMode==='bank'&&!paymentBankId){setError('Select a bank account for the invoice.');return}setBusy(true);setError('');const itemPayload=lines.map((l,i)=>({...l,tax_rate_id:taxEnabled?l.tax_rate_id:'',sort_order:i}));const r=editId?await supabase.rpc('update_invoice_draft',{p_invoice_id:editId,p_customer_id:customerId,p_invoice_date:invoiceDate,p_due_date:dueDate,p_items:itemPayload,p_invoice_discount_type:discountEnabled?discountType:null,p_invoice_discount_value:discountEnabled?Number(discountValue||0):0,p_notes:notes||null,p_terms:terms||null,p_template_id:template||null}):await supabase.rpc('create_invoice_from_items',{p_business_id:ctx.business_id,p_customer_id:customerId,p_invoice_date:invoiceDate,p_due_date:dueDate,p_items:itemPayload,p_invoice_discount_type:discountEnabled?discountType:null,p_invoice_discount_value:discountEnabled?Number(discountValue||0):0,p_notes:notes||null,p_terms:terms||null});if(r.error){setError(r.error.message);setBusy(false);return}const invoiceId=String(r.data);if(template){const tr=await supabase.from('invoices').update({template_id:template}).eq('id',invoiceId).eq('business_id',ctx.business_id);if(tr.error){setError(tr.error.message);setBusy(false);return}}const selection=await supabase.rpc('set_invoice_payment_display',{p_invoice_id:invoiceId,p_payment_display_mode:paymentMode,p_bank_account_id:paymentMode==='bank'?paymentBankId:null});if(selection.error){setError(selection.error.message);setBusy(false);return}if(paymentMode==='online'){const link=await supabase.functions.invoke('create-payment-link',{body:{invoice_id:invoiceId}});if(link.error||link.data?.error){setError(link.error?.message||link.data?.error||'Unable to create payment link.');setBusy(false);return}}location.href=`/next-workspace/documents?type=invoice&id=${invoiceId}`};
 if(!ctx||!editLoaded)return <div className="grid min-h-screen place-items-center text-sm text-slate-500">Loading invoice editor…</div>;
 const ruleLabel=composition?'Composition GST: tax is not charged separately':taxEligible?`${taxRegime==='GST'?'GST registered':`${taxRegime} enabled`} · Tax profiles apply per item`:'No tax regime configured';
 return <main className="min-h-screen bg-[#fbfaff] p-4 text-slate-950 sm:p-7"><div className="mx-auto max-w-7xl"><header className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-violet-600">{editId?'Edit draft invoice':'New invoice'}</p><h1 className="text-3xl font-bold tracking-tight">Create an invoice</h1><p className="mt-1 text-sm text-slate-500">{ruleLabel}</p><div className="mt-3 flex flex-wrap items-center gap-2 text-[11px] font-semibold text-slate-500"><span className="rounded-full bg-violet-50 px-3 py-1 text-violet-700">1 Customer</span><span>→</span><span className="rounded-full bg-violet-50 px-3 py-1 text-violet-700">2 Items & tax</span><span>→</span><span className="rounded-full bg-violet-50 px-3 py-1 text-violet-700">3 Review</span><span>→</span><span className="rounded-full bg-violet-50 px-3 py-1 text-violet-700">4 Post</span></div></div><div className="sm:ml-auto flex gap-2"><Button secondary onClick={()=>location.href='/next-workspace/documents'}>Cancel</Button><Button disabled={busy||!customerId||!lines.length||totals.total<=0||(paymentMode==='bank'&&!paymentBankId)} onClick={saveInvoice}>{busy?'Saving…':editId?'Save draft & review':'Save draft & review'}</Button></div></header>
 {error&&<Card className="mb-4 border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</Card>}
 <div className="grid gap-5 lg:grid-cols-[1fr_360px]"><div className="space-y-5">
  <Card className="p-5"><div className="grid gap-4 sm:grid-cols-2"><Field label="Customer" required wide><div className="flex gap-2"><Select value={customerId} onChange={e=>selectCustomer(e.target.value)}><option value="">Select customer…</option>{customers.map(c=><option key={c.id} value={c.id}>{c.display_name}</option>)}</Select><Button secondary onClick={()=>setCustomerFormOpen(true)}>New</Button></div></Field><Field label="Invoice date" required><Input type="date" value={invoiceDate} onChange={e=>setInvoiceDate(e.target.value)}/></Field><Field label="Due date" required><Input type="date" value={dueDate} onChange={e=>setDueDate(e.target.value)}/></Field></div></Card>
  <Card className={`${taxEnabled?'border-emerald-100 bg-emerald-50/40':'border-amber-100 bg-amber-50/40'} p-5`}><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className={`font-bold ${taxEnabled?'text-emerald-950':'text-amber-950'}`}>{taxEnabled?'GST / Tax on this invoice':'Tax configuration'}</h2><p className={`mt-1 text-xs ${taxEnabled?'text-emerald-800':'text-amber-800'}`}>{taxEnabled?`Tax is enabled for this ${taxRegime} business. Select the applicable rate in each item row.`:'Set the business tax regime/registration first, then create or manage GST/custom tax rates. If no tax is configured, the invoice remains tax-free.'}</p></div><div className="flex shrink-0 gap-2"><Button secondary onClick={()=>location.href='/next-workspace/tax'}>Manage tax profiles</Button>{gstEligible&&<button type="button" role="switch" aria-checked={gstEnabled} onClick={()=>setGstEnabled(v=>!v)} className={`relative h-7 w-12 shrink-0 rounded-full transition ${gstEnabled?'bg-emerald-600':'bg-slate-300'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition ${gstEnabled?'left-6':'left-1'}`}/></button>}</div></div></Card>
  {composition&&<Card className="border-amber-100 bg-amber-50/50 p-5"><h2 className="font-bold text-amber-950">Composition GST</h2><p className="mt-1 text-xs text-amber-800">GST is not charged separately on this invoice under the composition scheme.</p></Card>}
  <Card className="overflow-hidden"><div className="flex flex-col gap-3 border-b border-slate-100 px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-bold">Items</h2><p className="text-xs text-slate-500">Select an existing product or service. Description, quantity and rate are prefilled and remain editable.</p></div><div className="flex gap-2"><Button secondary onClick={()=>setItemChooserOpen(true)}>Create new product or service</Button><button type="button" aria-label="Add invoice line" title="Add line" onClick={()=>{if(sortedItems.length){addLine(sortedItems[0].item_type==='service'?'service':'product')}else setItemChooserOpen(true)}} className="grid h-10 w-10 place-items-center rounded-xl border border-slate-200 bg-white text-xl font-semibold text-violet-700 hover:bg-violet-50">+</button></div></div><div className="overflow-x-auto"><table className="min-w-[900px] w-full text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="p-3 text-left">Item</th><th className="p-3 text-left">Description</th><th className="p-3 text-right">Qty</th><th className="p-3 text-right">Rate</th>{taxEnabled&&<th className="p-3 text-left">GST / Tax rate</th>}<th className="p-3 text-right">Amount</th><th className="p-3"/></tr></thead><tbody>{lines.map((line,i)=>{const p=products.find(x=>x.id===line.product_service_id);const base=Number(line.quantity||0)*Number(line.unit_price||0);const d=lineDiscount(line,p);const rate=taxEnabled?(taxes.find(t=>t.id===line.tax_rate_id)?.rate||0):0;return <tr key={`${i}-${line.product_service_id}`} className="border-t border-slate-100 align-top"><td className="p-3"><Select value={line.product_service_id} onChange={e=>{const np=products.find(x=>x.id===e.target.value);changeLine(i,'product_service_id',e.target.value);if(np){changeLine(i,'description',np.description||np.name);changeLine(i,'quantity',1);changeLine(i,'unit_price',Number(np.sales_price||0));changeLine(i,'tax_rate_id',taxEnabled?(np.default_tax_rate_id||''):'')}}}><option value="">Select item…</option>{sortedItems.map(x=><option key={x.id} value={x.id}>{x.item_type==='service'?'Service':'Product'} · {x.name}{x.sku?` · ${x.sku}`:''}</option>)}</Select></td><td className="p-3"><Input value={line.description} onChange={e=>changeLine(i,'description',e.target.value)}/></td><td className="p-3 w-24"><Input type="number" min="0.001" step="0.001" value={line.quantity} onChange={e=>changeLine(i,'quantity',Number(e.target.value))}/></td><td className="p-3 w-32"><Input type="number" min="0" step="0.01" value={line.unit_price} onChange={e=>changeLine(i,'unit_price',Number(e.target.value))}/></td>{taxEnabled&&<td className="p-3 w-40"><Select value={line.tax_rate_id} onChange={e=>changeLine(i,'tax_rate_id',e.target.value)}><option value="">No GST / Tax</option>{taxes.map(t=><option key={t.id} value={t.id}>{t.name} · {t.rate}%</option>)}</Select></td>}<td className="p-3 text-right font-semibold">{money(Math.max(0,base-d+(base-d)*rate/100))}</td><td className="p-3 text-right"><button type="button" aria-label="Remove line" title="Remove line" onClick={()=>setLines(x=>x.filter((_,j)=>j!==i))} className="grid h-9 w-9 place-items-center rounded-lg border border-slate-200 text-rose-600 hover:bg-rose-50">×</button></td></tr>})}</tbody></table>{!lines.length&&<div className="p-10 text-center text-sm text-slate-500">No items yet. Use + to add a line or create a new product/service.</div>}</div></Card>
  <Card className="p-5"><div className="flex items-center justify-between gap-4"><div><h2 className="font-bold">Invoice discount</h2><p className="mt-1 text-xs text-slate-500">Enable only when this invoice has an overall discount. When disabled, the discount is always ₹0.</p></div><button type="button" role="switch" aria-checked={discountEnabled} onClick={()=>toggleDiscount(!discountEnabled)} className={`relative h-7 w-12 shrink-0 rounded-full transition ${discountEnabled?'bg-violet-600':'bg-slate-300'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition ${discountEnabled?'left-6':'left-1'}`}/></button></div>{discountEnabled&&<div className="mt-4 grid gap-4 sm:grid-cols-2"><Field label="Discount type" required><Select value={discountType||'percentage'} onChange={e=>setDiscountType(e.target.value)}><option value="percentage">Percentage</option><option value="amount">Amount</option></Select></Field><Field label="Discount value" required><Input type="number" min="0" value={discountValue} onChange={e=>setDiscountValue(Number(e.target.value))}/></Field></div>}</Card>
  <Card className="p-5"><div className="grid gap-4 sm:grid-cols-2"><Field label="Notes" wide><Input value={notes} onChange={e=>setNotes(e.target.value)}/></Field><Field label="Payment terms" wide><Input value={terms} onChange={e=>setTerms(e.target.value)}/></Field><Field label="Template"><Select value={template} onChange={e=>setTemplate(e.target.value)}><option value="">Business default</option>{templates.map(t=><option key={t.id} value={t.id}>{t.template_name}</option>)}</Select></Field></div></Card>
  <Card className="p-5 border-violet-100"><div className="flex items-start justify-between gap-4"><div><h2 className="font-bold">Payment display on invoice</h2><p className="mt-1 text-xs text-slate-500">Choose exactly what the customer sees on the printed, copied or shared invoice.</p></div><button type="button" role="switch" aria-checked={paymentMode!=='none'} onClick={()=>setPaymentMode(paymentMode==='none'?(banks.length?'bank':'online'):'none')} className={`relative h-7 w-12 shrink-0 rounded-full transition ${paymentMode!=='none'?'bg-violet-600':'bg-slate-300'}`}><span className={`absolute top-1 h-5 w-5 rounded-full bg-white shadow transition ${paymentMode!=='none'?'left-6':'left-1'}`}/></button></div>{paymentMode!=='none'&&<div className="mt-4 grid gap-3 sm:grid-cols-2"><button type="button" onClick={()=>setPaymentMode('bank')} className={`rounded-2xl border p-4 text-left ${paymentMode==='bank'?'border-violet-500 bg-violet-50 ring-2 ring-violet-100':'border-slate-200 bg-white'}`}><b className="block text-sm">Show bank details</b><span className="mt-1 block text-[11px] text-slate-500">Display the selected business bank account on the invoice.</span></button><button type="button" onClick={()=>setPaymentMode('online')} className={`rounded-2xl border p-4 text-left ${paymentMode==='online'?'border-violet-500 bg-violet-50 ring-2 ring-violet-100':'border-slate-200 bg-white'}`}><b className="block text-sm">Pay Now + payment QR</b><span className="mt-1 block text-[11px] text-slate-500">Uses the default backend payment gateway and generates the customer payment link/QR.</span></button></div>}{paymentMode==='bank'&&<div className="mt-4"><Field label="Bank account" required><Select value={paymentBankId} onChange={e=>setPaymentBankId(e.target.value)}><option value="">Select bank account…</option>{banks.map(b=><option key={b.id} value={b.id}>{b.name}{b.institution_name?` · ${b.institution_name}`:''}{b.account_last4?` ···${b.account_last4}`:''}</option>)}</Select></Field>{!banks.length&&<p className="mt-2 text-xs text-amber-600">No active bank accounts are configured. Add one in Document Settings → Bank Accounts.</p>}</div>}{paymentMode==='online'&&<div className="mt-4 rounded-2xl bg-slate-50 p-4 text-xs text-slate-600"><b className="text-slate-900">Automatic payment gateway</b><p className="mt-1">When you save the invoice, Moneymatters creates or reuses the backend payment link. The same link becomes the Pay Now action and QR payload on the invoice.</p></div>}</Card>
 </div><Card className="h-fit p-5 lg:sticky lg:top-5"><h2 className="font-bold">Invoice summary</h2><div className="mt-4 space-y-3 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{money(totals.subtotal)}</span></div><div className="flex justify-between"><span>Discount</span><span>-{money(totals.discount)}</span></div>{taxEnabled&&<div className="flex justify-between"><span>GST</span><span>{money(totals.tax)}</span></div>}<div className="border-t pt-3 flex justify-between text-lg font-bold"><span>Total</span><span>{money(totals.total)}</span></div></div>{customer&&<div className="mt-5 rounded-2xl bg-slate-50 p-4 text-xs text-slate-600"><div className="font-semibold text-slate-900">{customer.display_name}</div><div>Payment terms: {customer.payment_terms_days||defaults.invoice_due_days||0} days</div>{customer.phone&&<div>{customer.phone}</div>}{customer.email&&<div>{customer.email}</div>}{customer.website&&<div>{customer.website}</div>}</div>}{paymentMode!=='none'&&<div className="mt-4 rounded-2xl border border-violet-100 bg-violet-50/60 p-4 text-xs"><b className="text-violet-900">Invoice payment display</b><div className="mt-1">{paymentMode==='bank'?`Bank details${paymentBankId?` · ${banks.find(b=>b.id===paymentBankId)?.name||''}`:''}`:'Pay Now + QR'}</div></div>}</Card></div>
 {customerFormOpen&&<CustomerCreateModal open={customerFormOpen} onClose={()=>setCustomerFormOpen(false)} onCreated={onCustomerCreated} businessId={ctx.business_id}/>} {itemFormOpen&&<ItemServiceModal open={itemFormOpen} onClose={()=>setItemFormOpen(false)} onCreated={onItemCreated} businessId={ctx.business_id} initialItemType={newItemKind}/>} {itemChooserOpen&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/35 p-4"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-[11px] font-bold uppercase tracking-widest text-violet-600">Add to invoice</p><h2 className="mt-1 text-xl font-semibold">Create new product or service</h2><p className="mt-1 text-sm text-slate-500">Choose the type first; the full master-data form opens next.</p></div><button type="button" onClick={()=>setItemChooserOpen(false)} className="grid h-9 w-9 place-items-center rounded-xl bg-slate-100">×</button></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><button type="button" onClick={()=>openNewItem('product')} className="rounded-2xl border border-slate-200 p-5 text-left hover:border-violet-400 hover:bg-violet-50"><span className="text-xs font-bold uppercase tracking-wider text-violet-600">Product</span><b className="mt-2 block text-base">Create product</b><span className="mt-1 block text-xs text-slate-500">Stock, SKU, HSN, pricing and inventory options.</span></button><button type="button" onClick={()=>openNewItem('service')} className="rounded-2xl border border-slate-200 p-5 text-left hover:border-violet-400 hover:bg-violet-50"><span className="text-xs font-bold uppercase tracking-wider text-violet-600">Service</span><b className="mt-2 block text-base">Create service</b><span className="mt-1 block text-xs text-slate-500">Service unit, SAC, pricing and service details.</span></button></div></div></div>}
 </div></main>
}
